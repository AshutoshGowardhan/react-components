import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return <div>
        The First Project Of React 
    </div>
}

ReactDOM.render(<App />, document.querySelector("#root"));